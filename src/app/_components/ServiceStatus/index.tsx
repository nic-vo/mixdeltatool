import DialogWrapper from './_components/client';
import ServiceStatus from './_components/server';

const ServiceDialog = () => {
	return (
		<DialogWrapper>
			<ServiceStatus />
		</DialogWrapper>
	);
};

export default ServiceDialog;
